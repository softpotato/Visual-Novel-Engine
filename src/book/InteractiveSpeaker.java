package book;

/**
 * This class should work for both the 
 * @author Softp
 *
 */
public class InteractiveSpeaker {
	protected boolean[][] hitbox;	// might change later, though will say which coordinates have hit box.
	// Note: an alternative to a hit box is to use all pars that aren't transparent in the image
}
